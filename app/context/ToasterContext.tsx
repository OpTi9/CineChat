"use client";

import { Toaster } from "react-hot-toast";

/**
 * ToasterContext component.
 *
 * This is a functional component that renders the `Toaster` from
 * `react-hot-toast`. This Toaster is used to display toast
 * notifications in your application.
 *
 * @returns {JSX.Element} Returns a `Toaster` component for displaying toast notifications.
 */

const ToasterContext = () => {
  return <Toaster />;
};

export default ToasterContext;
